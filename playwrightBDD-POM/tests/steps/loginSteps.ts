import {createBdd} from "playwright-bdd";
import {test, expect} from "@playwright/test";

const {Given, When, Then} = createBdd();

Given('User is on login page', async ({}) => {
  
});

When('User enters username', async ({}) => {
  
});

When('User enter password', async ({}) => {
  
});

When('User clicks on login button', async ({}) => {
 
});

Then('User is logged in successfully', async ({}) => {
  
});

Then('User is able to navigate to product page', async ({}) => {
  
});

When('User enters locked_username', async ({}) => {
  
});

Then('User gets an error message', async ({}) => {
  
});

Then('User is back on login page', async ({}) => {
  
});